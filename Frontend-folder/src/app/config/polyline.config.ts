import { PolylineOptions } from "@models/gomap";

export const DEFAULT_POLYLINE: PolylineOptions = {
  strokeColor: '#FFD600',
  strokeOpacity: 0.2,
  strokeWeight: 5,
};

export const DEFAULT_POLYLINE_TRACKER: PolylineOptions = {
  strokeColor: '#00FF19',
  strokeOpacity: 0.8,
  strokeWeight: 8,
};
