/**
 * @interface PolylineOptions - Describes the style properties for a polyline
 */
export interface PolylineOptions {
  strokeColor: string;
  strokeOpacity: number;
  strokeWeight: number;
}
